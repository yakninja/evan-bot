commands:
  yum_update:
    command: yum update --assumeyes --quiet && yum install -y mc
container_commands:
  01_replace_supervisord_conf:
    command: cp -f .ebextensions/supervisord.conf /opt/python/etc/supervisord.conf && /usr/local/bin/supervisorctl -c /opt/python/etc/supervisord.conf reread
